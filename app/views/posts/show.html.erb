<h1><%= @post.title %></h1>
<h4>Created at: <%= @post.created_at %></h4>
<p><%= @post.text %></p>

  <%= render 'comments/form' %>
  <ul>
  <% @comments.each do |comment| %>
    <li><p><em>Comment posted at <%= comment.created_at %>:</em></p>
    <p><%= comment.text %></p></li>
  <% end %>
  </ul>
